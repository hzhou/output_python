macros:
    comment: #

subcode: basic_frame
    # I don't really see point of this, albeit works.
    $def main
        $call @main2

    $if __name__ == "__main__"
        main()

#---------------------------------------- 
include: ext.def
perlcode: quote
    my $var = $param
    $call grab_codelist
    $if $codelist
        push @$out, "$var = \"\"\"\n"
        $foreach $t in @$codelist
            push @$out, "$t\n"
        push @$out, "\"\"\"\n"


#---------------------
subcode: error(@msg)
    # $print stack: $stack
    raise Exception($(msg))

